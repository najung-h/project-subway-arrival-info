name: CI/CD Pipeline

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

env:
  REGISTRY: docker.io
  PROJECT_DIR: subwaylive

jobs:
  # 1️⃣ Build & Push
  build-and-push:
    runs-on: ubuntu-latest
    name: Docker 이미지 빌드 및 푸시

    steps:
      - name: 📦 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🪵 Debug Tree (Check project structure)
        run: |
          echo "📍 현재 경로 위치:"
          pwd
          echo "---------------------------"
          echo "📂 루트 디렉토리 구조"
          ls -al
          echo "---------------------------"
          echo "📁 프로젝트 디렉토리 구조 (${PROJECT_DIR})"
          ls -al "${PROJECT_DIR}" || echo "⚠️ ${PROJECT_DIR} 폴더가 존재하지 않습니다."
          echo "---------------------------"
          echo "✅ 디렉토리 확인 완료!"

          
      - name: 🧩 QEMU 설정 (멀티 아키텍처 지원)
        uses: docker/setup-qemu-action@v3

      - name: ⚙️ Buildx 설정
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Docker Hub 로그인
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 🏷️ 메타데이터 생성
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ secrets.IMAGE_NAME }}
          tags: |
            type=raw,value=latest
            type=sha

      - name: 🏗️ Docker 이미지 빌드 및 푸시
        uses: docker/build-push-action@v6
        with:
          context: ./${{ env.PROJECT_DIR }} 
          file: ./${{ env.PROJECT_DIR }}/docker/web.dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

  # 2️⃣ Deploy
  deploy:
    runs-on: ubuntu-latest
    name: EC2에 배포
    needs: build-and-push

    steps:
      - name: 📦 코드 체크아웃
        uses: actions/checkout@v4

      - name: 🚀 SSH 접속 후 배포
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_KEY }}
          script_stop: true
          script: |
            set -Eeuo pipefail

            APP_DIR=${{ secrets.APP_DIR }}
            PROJECT_DIR=subwaylive
            COMPOSE_DIR="$APP_DIR/$PROJECT_DIR/docker"
            COMPOSE_FILE="$COMPOSE_DIR/compose.prod.yml"
            IMAGE=${{ env.REGISTRY }}/${{ secrets.IMAGE_NAME }}
            SHA_TAG=${{ github.sha }}

            echo "📦 [0] Docker/Compose 점검 및 설치"
            if ! command -v docker >/dev/null 2>&1; then
              echo "→ Docker 미설치: 설치 진행 (Ubuntu)"
              # 공식 Docker 리포지토리 등록
              sudo apt-get update -y
              sudo apt-get install -y ca-certificates curl gnupg
              sudo install -m 0755 -d /etc/apt/keyrings
              curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg
              echo \
                "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] \
                https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable" \
                | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

              sudo apt-get update -y
              # docker engine + compose v2 플러그인 설치
              sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

              # 현재 사용자(예: ubuntu)를 docker 그룹에 추가 (선택)
              sudo usermod -aG docker $USER || true
              # 비대화식 세션에서는 그룹 재적용이 안 되므로, 아래에서는 계속 sudo 사용
            fi

            echo "📁 [1] 배포 디렉토리 준비: $APP_DIR"
            sudo mkdir -p "$APP_DIR"
            sudo chown -R "$USER":"$USER" "$APP_DIR"

            echo "🌀 [2] 리포지토리 최신화"
            if [ ! -d "$APP_DIR/.git" ]; then
              git clone --depth=1 https://github.com/${{ github.repository }} "$APP_DIR"
            else
              cd "$APP_DIR"
              git fetch --all --prune
              git reset --hard origin/${{ github.ref_name }}
            fi
            cd "$APP_DIR"

            echo "🔐 [3] .env 생성 → $APP_DIR/$PROJECT_DIR/.env"
            cat > "$APP_DIR/$PROJECT_DIR/.env" << 'EOF'
            DJANGO_SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }}
            DJANGO_DEBUG=False
            DJANGO_ALLOWED_HOSTS=${{ secrets.DJANGO_ALLOWED_HOSTS }}
            SEOUL_SUBWAY_API_KEY=${{ secrets.SEOUL_SUBWAY_API_KEY }}

            GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }}
            GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }}
            SITE_DOMAIN=${{ secrets.SITE_DOMAIN }}

            # RDS(MySQL)
            MYSQL_DATABASE=${{ secrets.MYSQL_DATABASE }}
            MYSQL_USER=${{ secrets.MYSQL_USER }}
            MYSQL_PASSWORD=${{ secrets.MYSQL_PASSWORD }}
            DB_HOST=${{ secrets.DB_HOST }}
            DB_PORT=${{ secrets.DB_PORT }}
            EOF

            echo "🐳 [4] 최신 이미지 Pull"
            sudo docker pull "$IMAGE:$SHA_TAG" || sudo docker pull "$IMAGE:latest"

            echo "🧩 [5] compose 파일 이미지 태그 치환"
            tmpfile=$(mktemp)
            sed "s|\(image: \).*|\1$IMAGE:$SHA_TAG|g" "$COMPOSE_FILE" > "$tmpfile"
            mv "$tmpfile" "$COMPOSE_FILE"

            echo "🚀 [6] Docker Compose 배포"
            cd "$COMPOSE_DIR"
            # compose v2: 'docker compose'
            sudo docker compose -f "$COMPOSE_FILE" up -d --remove-orphans

            echo "🛠️ [7] Django migrate / collectstatic / apply_socialapp"
            sudo docker compose -f "$COMPOSE_FILE" exec -T web \
              python manage.py migrate --settings=config.settings.prod
            sudo docker compose -f "$COMPOSE_FILE" exec -T web \
              python manage.py collectstatic --noinput --settings=config.settings.prod
            sudo docker compose -f "$COMPOSE_FILE" exec -T web \
              python manage.py apply_socialapp --settings=config.settings.prod

            echo "🧹 [8] 이미지 정리"
            sudo docker image prune -af || true

            echo "✅ 완료"
